# Production serving deps for the backend web service (Render).
# Deliberately slim: no torch/sentence-transformers (Jina covers embeddings),
# no evals stack, no Streamlit (widget replaces the chat UI).

fastapi
uvicorn[standard]
python-dotenv
requests
numpy
pytz
nest-asyncio
pydantic>=2.0.0
pydantic-settings

# Vector DB & retrieval
chromadb
rank-bm25

# Agent orchestration
langchain
langgraph
langgraph-checkpoint-postgres
langchain-community
langchain-openai
langchain-google-vertexai

# Guardrails
nemoguardrails
langchain-nvidia-ai-endpoints
pyyaml

# LLM gateway
portkey-ai
openai
tenacity

# Persistence / reliability
psycopg-binary
psycopg-pool
slowapi
redis
prometheus-client
prometheus-fastapi-instrumentator

# Observability
logfire[fastapi,requests,system-metrics]
loguru
langsmith
langfuse

# Document parsing (html/txt/pdf; office via unstructured)
beautifulsoup4
unstructured
python-pptx
python-docx
pypdf